import 'package:flutter/material.dart';

class PublicarPage extends StatelessWidget {
  const PublicarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Página para Publicar'),
    );
  }
}